# 🚀 QaraKino 快速开始指南

## Windows 用户（最简单）

### 第一步：安装
双击运行 `install.bat`，等待安装完成

### 第二步：启动
双击运行 `start.bat`

### 第三步：访问
打开浏览器访问：
- 用户端: http://localhost:3000
- 管理后台: http://localhost:3001

---

## Linux / macOS 用户

### 第一步：安装
```bash
chmod +x install.sh
./install.sh
```

### 第二步：启动
```bash
chmod +x start.sh
./start.sh
```

### 第三步：访问
打开浏览器访问：
- 用户端: http://localhost:3000
- 管理后台: http://localhost:3001

---

## 手动方式（高级用户）

### 1. 安装所有依赖
```bash
npm run install:all
```

### 2. 启动所有服务
```bash
npm run dev
```

---

## 下载页面

用浏览器打开 `download.html` 可以看到美观的下载页面！

---

## 常见问题

**Q: Node.js 没有安装？**
A: 请访问 https://nodejs.org 下载并安装 Node.js 16+

**Q: 端口被占用？**
A: 修改对应目录的 vite.config.js 或 server.js 中的端口号

**Q: 数据会保存吗？**
A: 当前版本使用内存存储，重启后数据会重置。生产环境请配置数据库。

**Q: 如何上传视频？**
A: 访问管理后台 http://localhost:3001，进入视频管理页面

---

## 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 用户端 | http://localhost:3000 | 维语视频平台 |
| 管理后台 | http://localhost:3001 | 内容管理系统 |
| 后端API | http://localhost:5000 | API 服务 |
| 下载页面 | 打开 download.html | 下载安装包 |

---

## 需要帮助？

查看 `README.md` 获取更多详细信息！
