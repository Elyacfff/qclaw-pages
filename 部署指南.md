# 🚀 QaraKino 免费部署指南

## 📦 准备部署文件

所有需要的文件都在 `/workspace/deploy/` 文件夹中，包含：

- ✅ 用户端（已构建）
- ✅ 管理后台（已构建）
- ✅ 后端服务器
- ✅ 部署配置文件

---

## 🎯 推荐的免费部署平台

### 方案一：Render（最推荐，完全免费）

**优点：**
- 完全免费，无需信用卡
- 自动部署
- 支持 Node.js 后端
- 支持 Git 仓库自动部署

**部署步骤：**

1. 访问 https://render.com/ 并注册账号
2. 在 GitHub/GitLab 上创建一个新仓库
3. 把 `deploy/` 文件夹的内容上传到仓库
4. 在 Render 上点击 "New > Web Service"
5. 选择你的仓库
6. 配置如下：
   - Name: `qarakino`
   - Region: 选择离你近的区域
   - Branch: `main`
   - Runtime: `Node`
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Plan: Free
7. 点击 "Create Web Service"
8. 等待几分钟，Render 会自动部署你的网站
9. 完成后，你会得到一个类似 `https://qarakino-abc123.onrender.com` 的网址

---

### 方案二：Railway（免费，有额度限制）

**优点：**
- 部署简单，支持拖放部署
- 免费额度足够日常使用
- 自动 HTTPS

**部署步骤：**

1. 访问 https://railway.app/ 并注册账号
2. 点击 "New Project"
3. 选择 "Deploy from GitHub repo" 或 "Deploy from directory"
4. 如果使用 GitHub，连接你的仓库（包含 deploy/ 内容）
5. 配置如下：
   - Name: `qarakino`
   - Build Command: `npm install`
   - Start Command: `node server.js`
6. 部署完成后，你会获得 `https://qarakino.up.railway.app` 链接

---

### 方案三：Koyeb（免费，有额度限制）

**优点：**
- 简单易用
- 免费额度
- 支持 Docker 部署

**部署步骤：**

1. 访问 https://www.koyeb.com/ 注册
2. 创建新服务，选择 "Git"
3. 连接你的仓库
4. 配置：
   - Build command: `npm install`
   - Run command: `node server.js`
   - Port: `3000`
5. 部署，获得域名

---

### 方案四：前端+后端分离部署（最灵活）

如果你想分开部署：

#### 前端部署（Vercel/Netlify）

**Vercel 部署用户端：**
1. 访问 https://vercel.com/
2. 拖放 `deploy/client/` 文件夹
3. 获得网址

**Vercel 部署管理后台：**
1. 创建另一个项目，拖放 `deploy/admin/` 文件夹
2. 获得网址

#### 后端部署（Render）

使用方案一部署后端，然后修改前端代码中的 API 地址

---

## 📱 部署完成后你会获得

| 服务 | 地址示例 |
|------|---------|
| **用户端** | `https://你的域名/` |
| **管理后台** | `https://你的域名/admin/` |
| **API 接口** | `https://你的域名/api/` |
| **健康检查** | `https://你的域名/api/health` |

---

## 🔑 管理后台登录信息

- 用户名: `admin`
- 密码: `admin123`

---

## 💡 快速上手（一分钟部署）

如果你有 GitHub 账号，使用 **Render** 方案最快：

1. 把 `/workspace/deploy/` 里的内容推送到 GitHub
2. 在 Render.com 连接你的仓库
3. 一键部署，3 分钟后上线！

---

## 📋 技术说明

- 使用内存存储（演示版本）
- 支持视频和缩略图上传
- 包含完整的 API
- 使用 React + Vite + Tailwind CSS

---

## ⚠️ 注意事项

1. **数据持久化**: 当前版本使用内存存储，服务重启后数据会重置。生产环境建议使用数据库。
2. **免费额度**: 免费平台通常有资源限制（内存、CPU），适合演示用途。
3. **安全**: 默认账号密码是 `admin/admin123`，生产部署请务必修改！

---

## 🚀 现在就开始部署吧！

所有文件都准备好，只需要把 `deploy/` 文件夹上传到 GitHub，然后在 Render.com 点击部署即可！
